1교시
형상관리 프로그램
csv-
svn-서버, 클라이언트
*git-서버, 저장소, 소스 - 포토샵, 엑셀도 이력 관리
TortoiseGit- git 클라이언트
TortoiseGit git path 경로잡기

node는 언어가 아니라 서버이다. 뷰 리엑트 앵귤러가 노드를 쓴다.
vs insider말고 일반을쓰자 학원에선 충돌방지용이다.

2교시
vue.js. devtools chrome 설치
vscode 플러그인
open in browser - 파일형태로 실행
설정등
코딩은 그냥 입력후 엔터 누르면 된다

.git에서 커밋등 모든 정보가 저장된다

3교시
mpa : url 여러개, 계산까지서버가전부다
spa : url 한개, 라우터, 비동기, 계산은 클라이언트가, 서버는 데이터만
mpa가 spa로 바뀌는 중이다.

웹의 이해
html css js로 만듦 결국 최종산출물은 html
node.js spa방식, 서버는 전달만 클라이언트가 계산

웹서버 : 정적인 데이터(이미지, css, js)등
was서버 : 동적인이미지(게시글)등

webpack 파일결합기, 플러그인보다 우월
spa는  es6, npm, webpack 기술이 필요.

함수1개, 
함수여러개모아놓은게 모듈(파일)
모듈 모아놓은게 패키지(디렉토리)
디렉토리가 여러개있는게(라이브러리)
라이브러리가여러개+new없이 인스턴스 만드는 기능도포함, 라이브러리보다 기능이 많은것
라이브러리와 프레임워크의 명확한 구분은 ioc

babel의 목적은 es6를 es5로 번역

css선택자
속성으로 찾을거면[ ]로 찾음

4교시
개발 : 유지보수 = 2 : 8
뷰 모델 디자인 패턴
mvp, mvc - 한방향
mvvm, mvw - 양방향
vue는 mvvm, flux 를 쓴다.

vue는 앵귤러와 리텍트의 좋은점을 결합
지시자와 컴퍼넌트가 중요

virutal dom 바뀐놈만 그린다.빠름
양방향이기 때문에 데이터가 바뀌면 화면이 자동으로 바뀜
꼬일수 있고 관리가 어렵기떄문에 데이터는 일단 단방향으로 처리한다.
양방향은 특별케이스외 처리 ㄴ

직접접근하지않고 중간에 함수를 통해접근하는것을 게이트키버기법이라함
getset기법이라고도 함

5교시
컴포넌트 :개발이 빨라진다.

vue의 핵심은 지시자
바인딩이 지시자 사용
컴퍼넌트는 화면 쪼개기
컴퍼넌트에서 젤 중요한건 데이터

computed 내부적처리
watch외부적처리

vue는 무조건 new vue 부터 시작

babel이란 JS컴파일러(번역기)이다. es6->es5로 바꿔줌

https://babeljs.io/repl
바벨 바뀐거 보는사이트 2015로한다,.

웹팩은 inport 방식으로 파일 결합

6교시
레퍼런스값은 직접바꿀떄 화면에서안바뀜 
Vue.set(object, key, value)를 이용해 바꾸자

기다리는거 : 동기식 computed
안기다리고 다른곳가서 대기 : 비동기식 watch
수동실행, 이벤트처리 : method

f12 vue탭도 명확하게나와서 개발에 유용하다.

반응성 규칙
var a = "1"
var a = "2"
여기서 a의 지속값은 다르다.
a들은 stack에 만들어짐
1,2는 heap에 만들어짐
a의 주소가 1000이라고 가정한다.
여기서 heap에 들어올 경우 1,2의 주소값은 1000이 아니다
다 다르다
주소가 변경이 일어나서 데이터가 바뀐것으로 인식한다고한다.
(아마 저렇게하면 a는 주소가안바뀐다고인식해서 화면에서 안바뀌는것같다)
그래서 vue.set을 써야한다.

명사 = 변수
동사 = 메소드
명사+동사=클래스 <-캡슐화
정보은닉(privite 등) 된 것들을 getter setter으로 들어간다.

자바스크립트는 두가지의 방식으로만듬
arr = [1,2,3]; 배열 리터럴
var arr = new Array(1,2,3); 생성자함수, 애는 array라는 프로토타입이 있다
Array의 부모는 Array.prototype이고 Array.prototype의 부모는 object.prototype이다.

vue() 위에는 vue.prototype, 그 위에는 fucntion.prototype, 그 부모는 object.prototype이다.

7교시
메소드를 찾는 방식은 프로토타입 체인이다.
결국 프로토타입에서 찾아쓰기떄문에
this.set과 vue.set은 같다.

v-가 붙은애들은 인스턴스에 설정해줘야한다(메소드로)

장시간사용하는 서버와의 비동기 통신에  watch를 사용한다.

computed로 정의하는 함수는 반드시! 리턴해야한다.
